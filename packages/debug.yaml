debug:
  update_interval: 30s

text_sensor:
  - platform: debug
    reset_reason:
      name: "Reboot Reason"
      entity_category: diagnostic

sensor:
  - platform: debug
    free:
      name: "Heap Free"
      entity_category: diagnostic