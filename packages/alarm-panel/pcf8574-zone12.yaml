binary_sensor:

  - id: zone12
    name: Zone 12
    pin:
      pcf8574: inputs_9_16
      number: $zone12
      mode: INPUT
      inverted: true
    platform: gpio   
    filters:
      - delayed_on_off: $sensor_debounce_time
    on_state:
      - if:
          condition:
            lambda: return id(blink_on_state);
          then:
            - script.execute: blink_status_led